#ifndef UTILS_H
#define UTILS_H

#include <fstream>
#include <iostream>
#include <sstream>
#include <string>
#include <vector>

using namespace std;

namespace utils {
void quicksort(vector<int> &arr, int low, int high);
int abs(int value);
} // namespace utils

#endif // UTILS_H
